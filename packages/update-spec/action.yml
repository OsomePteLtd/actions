name: 'Update spec'
description: 'Updates specs, commits and pushes updated types'
inputs:
  token:
    description: 'Github Token'
    required: true
runs:
  using: 'node12'
  main: './dist/index.js'
